# Café People Detection

This project fine-tunes a pre-trained Faster R-CNN model to detect people in a cafe
environment under varying lighting conditions.

## Setup
```bash
pip install -r requirements.txt
